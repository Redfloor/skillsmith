# A skill with no frontmatter at all

This file has no YAML frontmatter, so it is missing the required `name` and
`description` fields. It also references a file that does not exist:
see [the missing helper](reference/does-not-exist.md).

Convert the CSV to JSON and sort the rows by the first column.
